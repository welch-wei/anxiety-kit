# 焦虑急救箱 - 支付接入指南

## 已接入方案：支付宝个人收款码 + 兑换码核销（当前运行中）

### 用户路径
1. 打开焦虑急救箱 → 点击底部「升级 Pro」
2. 选择月卡 ¥9.9 或年卡 ¥69
3. 点击「支付宝扫码升级」
4. 截图收款码 → 打开支付宝扫一扫 → 付款
5. 加微信 `welch-wei` 发送支付截图，领取 8 位兑换码
6. 在页面输入兑换码 → 立即解锁 Pro

### 运营路径
1. 用户付款到支付宝后，你收到到账通知
2. 生成一个 8 位兑换码（如 `ANXIETY1`）
3. 把兑换码加入 `validProCodes` 列表（见下方）
4. 把兑换码通过微信发给用户
5. 用户兑换后，Pro 自动激活 30 天（从兑换时开始算）

### 如何添加新的有效兑换码
在浏览器控制台执行：
```javascript
const codes = JSON.parse(localStorage.getItem('validProCodes') || '[]');
codes.push('你的新兑换码');
localStorage.setItem('validProCodes', JSON.stringify(codes));
```

> 注意：localStorage 只保存在当前浏览器。如果你需要跨浏览器/设备管理兑换码，可以改用以下任一方案：
> - 使用一个共享的 JSON 文件放在 GitHub 仓库（手动更新，用户刷新页面即可生效）
> - 使用 Notion / Airtable / Google Sheets 作为兑换码后端（需要服务器或 Cloudflare Workers）
> - 接入有赞、小鹅通等 SaaS 自动发货

---

## 替代方案 A：支付宝开放平台（自动收款，需资质）

### 前置条件
- 企业支付宝账号或个体工商户
- 营业执照
- 备案域名
- 后端服务器

### 接入能力
- 手机网站支付（H5 支付）
- 电脑网站支付
- 当面付

### 接入流程
1. 登录 [支付宝开放平台](https://open.alipay.com/)
2. 创建应用 → 申请「手机网站支付」能力
3. 配置应用公钥、私钥、支付宝公钥
4. 后端生成订单签名，返回 form 或跳转 URL
5. 用户支付成功后，支付宝通过异步通知（notify_url）回调后端
6. 后端根据回调自动发放 Pro（写入数据库或调用兑换码接口）

### 优点
- 全自动，无需人工发码
- 支持退款、对账、发票
- 适合规模化

### 缺点
- 需要营业执照
- 需要域名备案
- 需要后端开发

---

## 替代方案 B：Stripe 订阅（海外/有资质）

### 1. 注册 Stripe 账号
- https://stripe.com
- 完成身份验证（个人护照/公司资料）
- 建议用香港/美国公司，个人账户较难过审

### 2. 创建产品和价格
- Products → 创建 Product：焦虑急救箱 Pro 月度
- 创建 Price：¥9.9 / month，按月订阅
- 再创建 Product：焦虑急救箱 Pro 年度，¥69 / year
- 复制两个 Price ID（格式 `price_xxxxxx`）

### 3. 填写配置
在 `index.html` 中找到：
```javascript
const STRIPE_CONFIG = {
  publishableKey: 'pk_test_xxxxxxxxxxxxxxxxxxxxxxxx',
  monthPriceId: 'price_xxxxxxxxxxxxxxxx',
  yearPriceId: 'price_yyyyyyyyyyyyyyyy'
};
```
替换为真实值。

### 4. 测试支付
- 使用 Stripe 测试卡：`4242 4242 4242 4242`，任意未来日期，任意 CVC
- 成功后 Stripe 会跳回 `?pro=success`

### 5. 上线收款
- 切换到生产模式：使用 `pk_live_xxx` 和 `price_xxx`（live）
- 在 Stripe Dashboard 中设置 Webhook，监听 `checkout.session.completed` 事件
- 根据 Webhook 自动开通 Pro

---

## 替代方案 C：第三方聚合支付（无后端）

- Paddle：https://paddle.com（支持订阅，税率自动处理）
- Lemon Squeezy：https://lemonsqueezy.com
- Gumroad：https://gumroad.com

这些平台提供托管结账页面，你只需复制链接/按钮即可。

---

## 当前实现状态

- ✅ 支付宝收款码已替换为真实图片
- ✅ 用户可点击「支付宝扫码升级」查看收款码
- ✅ 兑换码核销逻辑已接入
- ✅ 兑换成功后自动激活 Pro 30 天
- ⬜ 后续可考虑：自动回调、微信收款码、企业支付宝 H5 支付

---

## 收款账号与联系方式

- 支付宝收款码：`assets/alipay-qr.jpg`
- 客服微信：`welch-wei`
- 定价：Pro 月卡 ¥9.9 / 年卡 ¥69

